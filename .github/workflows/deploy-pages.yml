name: Deploy Pages

on:
  push:
    branches: [main]
    paths:
      - 'slides/**'
      - 'Makefile'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: slides/*/package-lock.json

      # Decks are auto-discovered by the Makefile: any slides/<slug>/ with a
      # package.json builds as Slidev, any with a top-level index.html is copied
      # as a static deck. Adding a talk needs no change to this workflow.
      - name: List discovered decks
        run: make list

      - name: Build and stage every deck into _site/
        run: make site

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
