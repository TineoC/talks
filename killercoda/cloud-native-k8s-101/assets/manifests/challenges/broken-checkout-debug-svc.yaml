apiVersion: v1
kind: Service
metadata:
  name: checkout-debug
  namespace: shop
  labels:
    app: checkout
    challenge: "step5"
spec:
  type: ClusterIP
  # BROKEN on purpose — selector typo means zero Endpoints
  selector:
    app: checkou
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
